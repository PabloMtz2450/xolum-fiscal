import{NextResponse,type NextRequest}from'next/server';const W=60_000,M=120,b=new Map<string,{n:number;reset:number}>();export function secureResponse(r:NextResponse){for(const[k,v]of Object.entries({'X-Content-Type-Options':'nosniff','X-Frame-Options':'DENY','Referrer-Policy':'no-referrer','Permissions-Policy':'camera=(), microphone=(), geolocation=()','Content-Security-Policy':"default-src 'self'; frame-ancestors 'none'; object-src 'none'; base-uri 'self'; form-action 'self'",'Strict-Transport-Security':'max-age=31536000; includeSubDomains','Cache-Control':'no-store'}))r.headers.set(k,v);return r}export function rateLimit(req:NextRequest){const k=req.headers.get('x-forwarded-for')?.split(',')[0]?.trim()||'unknown',n=Date.now(),x=b.get(k);if(!x||x.reset<n){b.set(k,{n:1,reset:n+W});return true}if(x.n>=M)return false;x.n++;return true}
