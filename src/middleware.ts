import{NextResponse,type NextRequest}from'next/server';import{rateLimit,secureResponse}from'./infrastructure/request-security';export function middleware(req:NextRequest){if(!rateLimit(req))return secureResponse(NextResponse.json({error:'Demasiadas solicitudes.'},{status:429}));return secureResponse(NextResponse.next())}export const config={matcher:['/((?!_next/static|_next/image|favicon.ico).*)']};
